# Botanize Botanize

## How do I install these formulae?

`brew install botanize/botanize/<formula>`

Or `brew tap botanize/botanize` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
